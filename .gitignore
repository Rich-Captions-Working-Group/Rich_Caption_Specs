# Node dependencies
node_modules/
package-lock.json

# Spec-Up build output
# (Since GitHub Actions will build this automatically, we don't need to commit it)
build/

# OS generated files
.DS_Store
Thumbs.db